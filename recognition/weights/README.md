"""
Weights directory for trained recognition models

Place your trained ResNet50 model checkpoint here:
- model_for_inference.pth  (main recognition model)
- Other checkpoint files as needed

The checkpoint should contain:
- model_state_dict
- config (num_classes, etc.)
- idx_to_brand mapping
- brand_to_idx mapping
- test_accuracy (optional)
"""
